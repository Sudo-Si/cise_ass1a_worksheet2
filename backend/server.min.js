const express=require("express"),server=express();server.set("view engine","ejs"),server.use(express.urlencoded({extended:!0})),server.use(express.json()),server.get("/",(req,res)=>{res.send("API is running")}),server.get("/test",(req,res)=>{res.send("test is working")}),server.get("api/articles",(req,res)=>{res.send("Articles"),res.json(articles)}),server.listen(5e3,console.log("Server is Working... and listening on PORT:5000"));